<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <title><%= title %></title>
</head>
<body>
    <div class="allMsg">
        <div class="miniAllMsg">
        <% if (messages && messages.length > 0) { %>
          <h2>The veil has lifted... truths once hidden are now yours to behold! 🌠</h2>
              <% messages.forEach(msg => { %>
                <% if (user && user.membershipstatus) { %>
                    <div class="tinyAllMsg">

                    <div class="superTinyAllMsg">
                    <h3><%= msg.title %></h3>
                    <p>By - <%= msg.author %></p>
                    </div>

                    <div class="superTinyAllMsg">
                    <h4>: <%= msg.text %></h4>
                    <p><%= msg.created_at %></p>
                    </div>
      
                    <% if (user && user.isadmin) { %>
                      <a href="/delete/<%= msg.id %>" onclick="return confirm('Are you sure you want to delete this message?');">🗑️</a>
                    <% } %>
                    </div>
               <% } %>
              <% }) %>
              <% } else { %>
                <p style="font-size: 1.1vw; font-family: 'Poppins', sans-serif;">No messages to show!</p>
              <% } %>   
              
              <a href="/">⬅ Back to Home</a>
        </div>      
      </div>
</body>
</html>